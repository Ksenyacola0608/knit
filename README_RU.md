# 🎨 HandyCraft - Платформа услуг ручного труда

## 📋 Содержание
- [О проекте](#о-проекте)
- [Технологический стек](#технологический-стек)
- [Запуск на платформе Emergent](#запуск-на-платформе-emergent)
- [Локальный запуск](#локальный-запуск)
- [Структура проекта](#структура-проекта)
- [API Документация](#api-документация)
- [Функциональность](#функциональность)

---

## 🎯 О проекте

HandyCraft - это веб-платформа для заказа услуг мастеров ручного труда (рукоделия). Пользователи могут заказывать услуги (например, вязание носков, вышивка), а мастера предоставляют свои услуги, получают заказы, рейтинги и отзывы.

**Аналог**: profi.ru для мастеров рукоделия

---

## 🛠 Технологический стек

### Backend
- **Framework**: FastAPI 0.110.1
- **База данных**: MongoDB 4.5
- **ODM**: Motor (async MongoDB driver)
- **Валидация**: Pydantic v2
- **Авторизация**: JWT (python-jose, passlib[bcrypt])
- **Python**: 3.11+

### Frontend
- **Framework**: React 19
- **Язык**: JavaScript
- **UI библиотека**: Shadcn/UI + Radix UI
- **Стилизация**: Tailwind CSS
- **Роутинг**: React Router DOM v7
- **HTTP клиент**: Axios
- **Иконки**: Lucide React

### Инфраструктура
- **Контейнеризация**: Docker
- **Оркестрация**: Kubernetes
- **Управление процессами**: Supervisor
- **Веб-сервер**: Nginx (для проксирования)

---

## 🚀 Запуск на платформе Emergent

### Проект УЖЕ ЗАПУЩЕН!

Ваше приложение развернуто и работает на платформе Emergent:

**URL приложения**: https://crafty-connect.preview.emergentagent.com

### Текущий статус сервисов:
```bash
✅ backend    - RUNNING (порт 8001)
✅ frontend   - RUNNING (порт 3000)
✅ mongodb    - RUNNING (порт 27017)
✅ nginx      - RUNNING
```

### Доступ к приложению:
- **Frontend**: https://crafty-connect.preview.emergentagent.com
- **Backend API**: https://crafty-connect.preview.emergentagent.com/api
- **Health Check**: https://crafty-connect.preview.emergentagent.com/api/health

### Проверка работы:
```bash
# В терминале Emergent
curl http://localhost:8001/api/health
# Ответ: {"status":"healthy","database":"connected"}

# Проверить статус всех сервисов
sudo supervisorctl status
```

---

## 💻 Локальный запуск

### Предварительные требования

Для запуска проекта на вашем компьютере нужно установить:

1. **Python 3.11+**
   ```bash
   python --version  # Проверка версии
   ```

2. **Node.js 18+ и Yarn**
   ```bash
   node --version   # Проверка версии
   yarn --version
   ```

3. **MongoDB 4.5+**
   - **Windows**: Скачать с https://www.mongodb.com/try/download/community
   - **macOS**: `brew install mongodb-community`
   - **Linux**: 
     ```bash
     sudo apt-get install mongodb
     # или
     sudo yum install mongodb
     ```

### Шаг 1: Клонирование проекта

```bash
# Скачайте код проекта с Emergent или используйте git
git clone <ваш-репозиторий>
cd handcraft-platform
```

### Шаг 2: Настройка Backend

```bash
# Перейти в папку backend
cd backend

# Создать виртуальное окружение
python -m venv venv

# Активировать виртуальное окружение
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Создать файл .env (если его нет)
cat > .env << EOF
MONGO_URL="mongodb://localhost:27017"
DB_NAME="handcraft_platform"
CORS_ORIGINS="*"
JWT_SECRET="your-secret-key-change-in-production"
JWT_ALGORITHM="HS256"
JWT_EXPIRATION="1440"
EOF
```

### Шаг 3: Настройка Frontend

```bash
# Перейти в папку frontend (из корня проекта)
cd ../frontend

# Установить зависимости
yarn install

# Создать файл .env (если его нет)
cat > .env << EOF
REACT_APP_BACKEND_URL=http://localhost:8001
REACT_APP_ENABLE_VISUAL_EDITS=false
ENABLE_HEALTH_CHECK=false
EOF
```

### Шаг 4: Запуск MongoDB

```bash
# Windows (через Services или CMD):
net start MongoDB

# macOS:
brew services start mongodb-community

# Linux:
sudo systemctl start mongodb
# или
sudo service mongodb start

# Проверка работы MongoDB:
mongosh
# Если подключилось - MongoDB работает!
```

### Шаг 5: Запуск Backend

```bash
# В терминале 1 (из папки backend с активированным venv)
cd backend
uvicorn server:app --reload --host 0.0.0.0 --port 8001

# Вы должны увидеть:
# INFO:     Uvicorn running on http://0.0.0.0:8001 (Press CTRL+C to quit)
# INFO:     Started reloader process
```

**Проверка**: Откройте http://localhost:8001/api/health
Должен вернуться: `{"status":"healthy","database":"connected"}`

### Шаг 6: Запуск Frontend

```bash
# В терминале 2 (из папки frontend)
cd frontend
yarn start

# Вы должны увидеть:
# Compiled successfully!
# You can now view frontend in the browser.
# Local: http://localhost:3000
```

**Проверка**: Откройте http://localhost:3000

### ✅ Готово!

Теперь у вас работает:
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8001
- **API Docs**: http://localhost:8001/docs (Swagger UI)
- **MongoDB**: localhost:27017

---

## 🗂 Структура проекта

```
/app/
├── backend/                 # Backend приложение
│   ├── models/             # Pydantic модели
│   │   ├── user.py
│   │   ├── service.py
│   │   ├── order.py
│   │   ├── review.py
│   │   ├── message.py
│   │   └── notification.py
│   ├── routers/            # API роутеры
│   │   ├── auth.py
│   │   ├── users.py
│   │   ├── services.py
│   │   ├── orders.py
│   │   ├── reviews.py
│   │   ├── messages.py
│   │   └── notifications.py
│   ├── utils/              # Утилиты
│   │   ├── auth.py         # JWT токены
│   │   └── security.py     # Хеширование паролей
│   ├── database.py         # MongoDB подключение
│   ├── server.py           # Главный файл FastAPI
│   ├── requirements.txt    # Python зависимости
│   └── .env               # Переменные окружения
│
├── frontend/               # Frontend приложение
│   ├── src/
│   │   ├── components/    # React компоненты
│   │   │   ├── layout/   # Header, Footer
│   │   │   └── ui/       # Shadcn UI компоненты
│   │   ├── pages/        # Страницы приложения
│   │   ├── context/      # AuthContext
│   │   ├── utils/        # API client, auth utils
│   │   ├── constants/    # Константы
│   │   ├── App.js
│   │   └── index.js
│   ├── package.json
│   └── .env
│
├── docs/                   # Документация
│   ├── 01_technical_specification.md
│   ├── 02_architecture.md
│   ├── 03_data_models.md
│   ├── 04_api_endpoints.md
│   └── 05_ui_screens.md
│
└── README_RU.md           # Эта инструкция
```

---

## 📚 API Документация

### Доступ к документации

После запуска backend перейдите по адресам:
- **Swagger UI**: http://localhost:8001/docs
- **ReDoc**: http://localhost:8001/redoc

### Основные endpoints

#### Авторизация
```bash
# Регистрация
POST /api/auth/register
Body: {
  "email": "user@example.com",
  "password": "password123",
  "name": "Иван Иванов",
  "role": "customer"  # или "master"
}

# Вход
POST /api/auth/login
Body: {
  "email": "user@example.com",
  "password": "password123"
}
Response: {
  "token": "jwt-token",
  "user": {...}
}
```

#### Услуги
```bash
# Получить все услуги
GET /api/services?category=knitting&skip=0&limit=20

# Получить услугу по ID
GET /api/services/{service_id}

# Создать услугу (только мастера)
POST /api/services
Headers: Authorization: Bearer {token}
Body: {
  "title": "Вязание носков",
  "description": "Подробное описание",
  "category": "knitting",
  "price": 1500,
  "duration_days": 7
}
```

#### Заказы
```bash
# Создать заказ
POST /api/orders
Headers: Authorization: Bearer {token}
Body: {
  "service_id": "uuid",
  "description": "Описание заказа"
}

# Получить свои заказы
GET /api/orders?role=customer
Headers: Authorization: Bearer {token}

# Обновить статус заказа (только мастер)
PATCH /api/orders/{order_id}/status
Headers: Authorization: Bearer {token}
Body: {
  "status": "accepted",
  "agreed_price": 1800
}
```

Полная документация API находится в `/app/docs/04_api_endpoints.md`

---

## 🎨 Функциональность

### Для заказчиков:
- ✅ Регистрация и авторизация
- ✅ Просмотр каталога услуг
- ✅ Поиск и фильтрация услуг
- ✅ Создание заказов
- ✅ Отслеживание статуса заказов
- ✅ Чат с мастером
- ✅ Оставление отзывов
- ✅ Уведомления о статусе заказа

### Для мастеров:
- ✅ Создание профиля мастера
- ✅ Управление услугами (создание, редактирование)
- ✅ Просмотр входящих заказов
- ✅ Принятие/отклонение заказов
- ✅ Изменение статуса заказа
- ✅ Чат с заказчиком
- ✅ Просмотр своего рейтинга и отзывов
- ✅ Статистика выполненных заказов

---

## ❓ Частые проблемы и решения

### 1. Backend не запускается

**Проблема**: `ImportError` или `ModuleNotFoundError`
```bash
# Решение:
cd backend
pip install -r requirements.txt
```

**Проблема**: `Cannot connect to MongoDB`
```bash
# Решение: Проверьте, запущен ли MongoDB
# Windows:
net start MongoDB
# macOS/Linux:
sudo service mongodb status
```

### 2. Frontend не запускается

**Проблема**: `Cannot find module`
```bash
# Решение:
cd frontend
yarn install
```

**Проблема**: `REACT_APP_BACKEND_URL is not defined`
```bash
# Решение: Создайте файл frontend/.env с содержимым:
REACT_APP_BACKEND_URL=http://localhost:8001
```

### 3. CORS ошибки

**Проблема**: `Access-Control-Allow-Origin` error
```bash
# Решение: В файле backend/.env должно быть:
CORS_ORIGINS="*"
# И перезапустите backend
```

### 4. Изображения не отображаются

**Проблема**: Изображения в Base64 слишком большие
```bash
# Рекомендация: Сжимайте изображения до 500KB перед загрузкой
# В будущем можно мигрировать на S3/MinIO для хранения
```

---

## 🔧 Полезные команды

### Backend (FastAPI)
```bash
# Запуск с hot reload
uvicorn server:app --reload

# Запуск на определенном порту
uvicorn server:app --port 8000

# Запуск с логами
uvicorn server:app --log-level debug
```

### Frontend (React)
```bash
# Запуск dev сервера
yarn start

# Сборка production
yarn build

# Проверка кода
yarn lint
```

### MongoDB
```bash
# Подключение к MongoDB shell
mongosh

# Просмотр баз данных
show dbs

# Использовать базу handcraft_platform
use handcraft_platform

# Просмотр коллекций
show collections

# Найти всех пользователей
db.users.find()

# Очистить коллекцию (осторожно!)
db.users.deleteMany({})
```

---

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте логи:
   - Backend: В терминале где запущен uvicorn
   - Frontend: В терминале где запущен yarn start
   - MongoDB: `sudo tail -f /var/log/mongodb/mongod.log`

2. Проверьте, что все сервисы запущены:
   ```bash
   # Backend
   curl http://localhost:8001/api/health
   
   # Frontend
   curl http://localhost:3000
   
   # MongoDB
   mongosh
   ```

3. Изучите документацию в папке `/app/docs/`

---

## 🎉 Следующие шаги

После успешного запуска:

1. **Доработать Frontend**:
   - Создать недостающие страницы (HomePage, ServicesPage и др.)
   - Добавить формы создания услуг
   - Реализовать полный UI для чата
   
2. **Оптимизировать Backend**:
   - Исправить N+1 query проблемы (см. отчет deployment_agent)
   - Добавить индексы в MongoDB
   - Оптимизировать работу с изображениями

3. **Тестирование**:
   - Протестировать регистрацию и авторизацию
   - Создать тестовые данные (мастеров, услуг)
   - Проверить весь flow: регистрация → создание услуги → заказ → отзыв

4. **Деплой**:
   - На платформе Emergent уже развернуто!
   - Для production: настроить домен, SSL, безопасность

---

**Создано на платформе Emergent AI** 🚀
